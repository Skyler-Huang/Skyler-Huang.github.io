<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>黄乐天 - 个人简历</title>
    <style>
        :root {
            --bg: #faf7f2;
            --card-bg: #ffffff;
            --ink: #1e3c32;
            --ink-light: #2a5446;
            --ink-muted: #4a6a5e;
            --gold: #b68b40;
            --gold-light: #d4a855;
            --gold-pale: #f5ede0;
            --border-subtle: #e8e2d8;
            --shadow-sm: 0 1px 3px rgba(30, 60, 50, 0.06);
            --shadow-md: 0 4px 16px rgba(30, 60, 50, 0.08);
            --radius: 6px;
            --radius-lg: 10px;
            --transition: 0.25s ease;
            --font-mono: "SF Mono", "Cascadia Code", "Consolas", "Menlo", "Noto Sans Mono", monospace;
            --font-sans: "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", "Noto Sans SC", system-ui, -apple-system, sans-serif;
        }

        *,
        *::before,
        *::after {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        html {
            font-size: 16px;
            scroll-behavior: smooth;
            -webkit-text-size-adjust: 100%;
            text-size-adjust: 100%;
        }

        body {
            font-family: var(--font-sans);
            background-color: var(--bg);
            color: var(--ink);
            line-height: 1.7;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            padding: 0;
            margin: 0;
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
            /* 极微弱的纸张纹理 */
            background-image:
                radial-gradient(ellipse at 20% 10%, rgba(182, 139, 64, 0.03) 0%, transparent 60%),
                radial-gradient(ellipse at 80% 30%, rgba(30, 60, 50, 0.02) 0%, transparent 55%),
                radial-gradient(ellipse at 50% 70%, rgba(182, 139, 64, 0.025) 0%, transparent 50%);
        }

        /* ========== 主容器 ========== */
        .container {
            width: 100%;
            max-width: 860px;
            padding: 1.25rem 1rem 2.5rem;
            display: flex;
            flex-direction: column;
            gap: 1.5rem;
        }

        /* ========== 头部 ========== */
        .header {
            text-align: center;
            padding: 2rem 1.25rem 1.75rem;
            position: relative;
            background: var(--card-bg);
            border-radius: var(--radius-lg);
            box-shadow: var(--shadow-md);
            border: 1px solid var(--border-subtle);
            overflow: hidden;
        }

        /* 顶部装饰线 */
        .header::before {
            content: '';
            position: absolute;
            top: 0;
            left: 50%;
            transform: translateX(-50%);
            width: 60px;
            height: 3px;
            background: var(--gold);
            border-radius: 0 0 3px 3px;
        }

        /* 背景几何装饰 */
        .header::after {
            content: '';
            position: absolute;
            top: -40px;
            right: -30px;
            width: 140px;
            height: 140px;
            border: 1px solid rgba(182, 139, 64, 0.12);
            border-radius: 50%;
            pointer-events: none;
        }

        .header__decoration {
            position: absolute;
            bottom: 20px;
            left: 25px;
            width: 50px;
            height: 50px;
            border: 1px dashed rgba(30, 60, 50, 0.08);
            border-radius: 50%;
            pointer-events: none;
        }

        .header__decoration--right {
            left: auto;
            right: 28px;
            bottom: 16px;
            width: 36px;
            height: 36px;
            border-style: solid;
            border-color: rgba(182, 139, 64, 0.1);
        }

        .header__name {
            font-size: 2rem;
            font-weight: 700;
            color: var(--ink);
            letter-spacing: 0.06em;
            margin-bottom: 0.4rem;
            position: relative;
            z-index: 1;
        }

        .header__tagline {
            font-size: 1rem;
            color: var(--gold);
            font-weight: 500;
            letter-spacing: 0.04em;
            margin-bottom: 0.6rem;
            position: relative;
            z-index: 1;
        }

        .header__divider {
            width: 50px;
            height: 2px;
            background: var(--gold);
            margin: 0.5rem auto 0.75rem;
            opacity: 0.7;
            border-radius: 1px;
        }

        .header__school {
            font-size: 0.9rem;
            color: var(--ink-muted);
            letter-spacing: 0.03em;
            position: relative;
            z-index: 1;
            line-height: 1.6;
        }

        .header__school strong {
            color: var(--ink);
            font-weight: 600;
        }

        /* ========== 区块通用 ========== */
        .section {
            background: var(--card-bg);
            border-radius: var(--radius-lg);
            padding: 1.5rem 1.25rem;
            box-shadow: var(--shadow-sm);
            border: 1px solid var(--border-subtle);
            position: relative;
            transition: box-shadow var(--transition);
        }

        .section:hover {
            box-shadow: var(--shadow-md);
        }

        .section__title {
            font-size: 1.2rem;
            font-weight: 700;
            color: var(--ink);
            letter-spacing: 0.05em;
            margin-bottom: 1.15rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            position: relative;
        }

        /* 标题左侧古铜金标记 */
        .section__title::before {
            content: '';
            display: inline-block;
            width: 8px;
            height: 8px;
            background: var(--gold);
            border-radius: 2px;
            flex-shrink: 0;
            transform: rotate(45deg);
        }

        /* 标题下方细线 */
        .section__title::after {
            content: '';
            position: absolute;
            bottom: -6px;
            left: 0;
            width: 100%;
            height: 1px;
            background: linear-gradient(to right, rgba(182, 139, 64, 0.4), rgba(182, 139, 64, 0.05) 60%, transparent);
            border-radius: 1px;
        }

        /* ========== 教育背景 ========== */
        .edu__block {
            margin-bottom: 0.6rem;
        }

        .edu__block:last-child {
            margin-bottom: 0;
        }

        .edu__school {
            font-weight: 700;
            color: var(--ink);
            font-size: 1.02rem;
            letter-spacing: 0.03em;
        }

        .edu__detail {
            font-size: 0.9rem;
            color: var(--ink-muted);
            margin-top: 0.15rem;
            letter-spacing: 0.02em;
        }

        .edu__courses {
            display: flex;
            flex-wrap: wrap;
            gap: 0.4rem;
            margin-top: 0.55rem;
        }

        .edu__course-tag {
            display: inline-block;
            font-size: 0.8rem;
            padding: 0.25rem 0.65rem;
            border: 1px solid rgba(30, 60, 50, 0.2);
            border-radius: 3px;
            color: var(--ink-light);
            letter-spacing: 0.03em;
            background: rgba(30, 60, 50, 0.025);
            white-space: nowrap;
        }

        /* ========== 专业技能 ========== */
        .skills__group {
            margin-bottom: 0.85rem;
        }

        .skills__group:last-child {
            margin-bottom: 0;
        }

        .skills__group-title {
            font-weight: 700;
            font-size: 0.9rem;
            color: var(--gold);
            letter-spacing: 0.05em;
            margin-bottom: 0.4rem;
            text-transform: uppercase;
        }

        .skills__tags {
            display: flex;
            flex-wrap: wrap;
            gap: 0.4rem;
        }

        .skills__tag {
            display: inline-block;
            font-size: 0.82rem;
            padding: 0.3rem 0.7rem;
            border: 1px solid rgba(30, 60, 50, 0.25);
            border-radius: 3px;
            color: var(--ink);
            letter-spacing: 0.03em;
            background: transparent;
            transition: all var(--transition);
            white-space: nowrap;
        }

        .skills__tag--highlight {
            border-color: var(--gold);
            color: var(--gold);
            font-weight: 500;
            background: var(--gold-pale);
        }

        /* ========== 项目经历 ========== */
        .timeline {
            list-style: none;
            padding: 0;
            display: flex;
            flex-direction: column;
            gap: 1.1rem;
        }

        .timeline__item {
            position: relative;
            padding-left: 1.4rem;
            border-left: 2px solid rgba(182, 139, 64, 0.35);
            padding-bottom: 0.15rem;
        }

        .timeline__item::before {
            content: '';
            position: absolute;
            left: -6px;
            top: 4px;
            width: 10px;
            height: 10px;
            background: var(--gold);
            border-radius: 50%;
            border: 2px solid var(--card-bg);
            box-shadow: 0 0 0 2px rgba(182, 139, 64, 0.3);
        }

        .timeline__header {
            display: flex;
            flex-wrap: wrap;
            align-items: baseline;
            gap: 0.4rem 0.7rem;
            margin-bottom: 0.2rem;
        }

        .timeline__title {
            font-weight: 700;
            font-size: 0.98rem;
            color: var(--ink);
            letter-spacing: 0.03em;
        }

        .timeline__role {
            font-size: 0.82rem;
            color: var(--gold);
            font-weight: 500;
            letter-spacing: 0.04em;
        }

        .timeline__date {
            font-family: var(--font-mono);
            font-size: 0.78rem;
            color: var(--ink-muted);
            letter-spacing: 0.02em;
            white-space: nowrap;
        }

        .timeline__desc {
            font-size: 0.88rem;
            color: var(--ink-muted);
            line-height: 1.6;
            letter-spacing: 0.02em;
        }

        /* ========== 实习经历 ========== */
        .internship__card {
            padding: 0;
        }

        .internship__header {
            display: flex;
            flex-wrap: wrap;
            align-items: baseline;
            gap: 0.4rem 0.7rem;
            margin-bottom: 0.3rem;
        }

        .internship__title {
            font-weight: 700;
            font-size: 0.98rem;
            color: var(--ink);
            letter-spacing: 0.03em;
        }

        .internship__date {
            font-family: var(--font-mono);
            font-size: 0.78rem;
            color: var(--ink-muted);
            letter-spacing: 0.02em;
            white-space: nowrap;
        }

        .internship__desc {
            font-size: 0.88rem;
            color: var(--ink-muted);
            line-height: 1.6;
            letter-spacing: 0.02em;
        }

        /* ========== 实习与就业意向 ========== */
        .intent__grid {
            display: flex;
            flex-wrap: wrap;
            gap: 1rem;
        }

        .intent__item {
            flex: 1 1 200px;
            background: rgba(30, 60, 50, 0.025);
            border-radius: var(--radius);
            padding: 0.9rem 1rem;
            border: 1px solid rgba(30, 60, 50, 0.08);
        }

        .intent__label {
            font-size: 0.75rem;
            font-weight: 700;
            color: var(--gold);
            letter-spacing: 0.06em;
            text-transform: uppercase;
            margin-bottom: 0.3rem;
        }

        .intent__value {
            font-size: 0.9rem;
            color: var(--ink);
            letter-spacing: 0.03em;
            line-height: 1.5;
        }

        .intent__note {
            margin-top: 0.75rem;
            font-size: 0.84rem;
            color: var(--ink-muted);
            letter-spacing: 0.02em;
            line-height: 1.6;
            padding: 0.6rem 0.75rem;
            background: rgba(182, 139, 64, 0.05);
            border-radius: var(--radius);
            border-left: 3px solid var(--gold);
        }

        /* ========== 个人特质与兴趣 ========== */
        .traits__subtitle {
            font-weight: 700;
            font-size: 0.9rem;
            color: var(--ink);
            letter-spacing: 0.04em;
            margin-bottom: 0.45rem;
            margin-top: 0.2rem;
        }

        .traits__subtitle:first-child {
            margin-top: 0;
        }

        .traits__list {
            list-style: none;
            padding: 0;
            display: flex;
            flex-direction: column;
            gap: 0.4rem;
            margin-bottom: 1rem;
        }

        .traits__list-item {
            font-size: 0.88rem;
            color: var(--ink-muted);
            padding-left: 1.1rem;
            position: relative;
            letter-spacing: 0.02em;
            line-height: 1.5;
        }

        .traits__list-item::before {
            content: '◆';
            position: absolute;
            left: 0;
            top: 0;
            font-size: 0.45rem;
            color: var(--gold);
            line-height: 1.7;
        }

        .interests__tags {
            display: flex;
            flex-wrap: wrap;
            gap: 0.45rem;
        }

        .interests__tag {
            display: inline-block;
            font-size: 0.8rem;
            padding: 0.3rem 0.7rem;
            border: 1px solid rgba(30, 60, 50, 0.2);
            border-radius: 20px;
            color: var(--ink-light);
            letter-spacing: 0.03em;
            background: transparent;
            white-space: nowrap;
            transition: all var(--transition);
        }

        .interests__tag:hover {
            border-color: var(--gold);
            color: var(--gold);
            background: var(--gold-pale);
        }

        /* ========== 页脚 ========== */
        .footer {
            text-align: center;
            padding: 1rem 0 0;
            font-size: 0.75rem;
            color: rgba(30, 60, 50, 0.4);
            letter-spacing: 0.05em;
            font-family: var(--font-mono);
        }

        .footer__dot {
            display: inline-block;
            width: 4px;
            height: 4px;
            background: var(--gold);
            border-radius: 50%;
            margin: 0 0.5rem;
            vertical-align: middle;
            opacity: 0.5;
        }

        /* ========== 桌面端 ========== */
        @media (min-width: 768px) {
            .container {
                padding: 2.5rem 1.5rem 4rem;
                gap: 1.8rem;
            }

            .header {
                padding: 2.75rem 2rem 2.25rem;
            }

            .header__name {
                font-size: 2.6rem;
                letter-spacing: 0.08em;
            }

            .header__tagline {
                font-size: 1.1rem;
                letter-spacing: 0.06em;
            }

            .header__school {
                font-size: 0.95rem;
            }

            .header__divider {
                width: 60px;
            }

            /* 桌面端header装饰 */
            .header::before {
                width: 80px;
                height: 3px;
            }

            .header::after {
                top: -55px;
                right: -40px;
                width: 200px;
                height: 200px;
            }

            .section {
                padding: 1.8rem 1.75rem;
            }

            .section__title {
                font-size: 1.3rem;
                letter-spacing: 0.06em;
            }

            .section__title::before {
                width: 10px;
                height: 10px;
            }

            .timeline__item {
                padding-left: 1.8rem;
            }

            .timeline__item::before {
                left: -7px;
                width: 12px;
                height: 12px;
            }

            .timeline__title,
            .internship__title {
                font-size: 1.02rem;
            }

            .timeline__desc,
            .internship__desc {
                font-size: 0.9rem;
            }

            .skills__tag {
                font-size: 0.84rem;
                padding: 0.35rem 0.8rem;
            }

            .edu__course-tag {
                font-size: 0.82rem;
                padding: 0.3rem 0.75rem;
            }

            .intent__grid {
                gap: 1.25rem;
            }

            .intent__note {
                font-size: 0.86rem;
            }

            .traits__list-item {
                font-size: 0.9rem;
            }

            .interests__tag {
                font-size: 0.82rem;
                padding: 0.35rem 0.8rem;
            }
        }

        /* ========== 大桌面端 ========== */
        @media (min-width: 900px) {
            .container {
                padding: 3rem 0 5rem;
                gap: 2rem;
            }

            .header {
                padding: 3rem 2.5rem 2.5rem;
            }

            .header__name {
                font-size: 2.9rem;
            }

            .section {
                padding: 2rem 2rem;
            }

            .section__title {
                font-size: 1.35rem;
            }
        }

        /* ========== 小屏手机优化 (360px-400px) ========== */
        @media (max-width: 420px) {
            html {
                font-size: 15px;
            }

            .container {
                padding: 0.75rem 0.65rem 1.75rem;
                gap: 1.1rem;
            }

            .header {
                padding: 1.5rem 0.9rem 1.3rem;
                border-radius: var(--radius);
            }

            .header__name {
                font-size: 1.65rem;
                letter-spacing: 0.04em;
            }

            .header__tagline {
                font-size: 0.85rem;
                letter-spacing: 0.03em;
            }

            .header__school {
                font-size: 0.78rem;
            }

            .header__divider {
                width: 36px;
                margin: 0.4rem auto 0.6rem;
            }

            .header::before {
                width: 40px;
                height: 2px;
            }

            .header::after {
                width: 80px;
                height: 80px;
                top: -25px;
                right: -20px;
            }

            .header__decoration {
                display: none;
            }

            .section {
                padding: 1.1rem 0.9rem;
                border-radius: var(--radius);
            }

            .section__title {
                font-size: 1.05rem;
                letter-spacing: 0.04em;
                margin-bottom: 1rem;
            }

            .section__title::before {
                width: 6px;
                height: 6px;
            }

            .section__title::after {
                bottom: -4px;
            }

            .timeline__item {
                padding-left: 1.1rem;
                border-left-width: 1.5px;
            }

            .timeline__item::before {
                left: -5px;
                width: 8px;
                height: 8px;
                top: 3px;
            }

            .timeline__title,
            .internship__title {
                font-size: 0.9rem;
            }

            .timeline__role {
                font-size: 0.75rem;
            }

            .timeline__date,
            .internship__date {
                font-size: 0.7rem;
            }

            .timeline__desc,
            .internship__desc {
                font-size: 0.8rem;
            }

            .skills__tag {
                font-size: 0.74rem;
                padding: 0.22rem 0.5rem;
            }

            .skills__group-title {
                font-size: 0.8rem;
            }

            .edu__course-tag {
                font-size: 0.72rem;
                padding: 0.2rem 0.5rem;
            }

            .edu__school {
                font-size: 0.92rem;
            }

            .edu__detail {
                font-size: 0.8rem;
            }

            .intent__item {
                flex: 1 1 100%;
                padding: 0.7rem 0.8rem;
            }

            .intent__label {
                font-size: 0.7rem;
            }

            .intent__value {
                font-size: 0.82rem;
            }

            .intent__note {
                font-size: 0.76rem;
                padding: 0.5rem 0.65rem;
                border-left-width: 2px;
            }

            .traits__list-item {
                font-size: 0.8rem;
                padding-left: 0.9rem;
            }

            .traits__subtitle {
                font-size: 0.82rem;
            }

            .interests__tag {
                font-size: 0.72rem;
                padding: 0.22rem 0.55rem;
            }

            .footer {
                font-size: 0.68rem;
            }
        }

        /* ========== 极小屏 (360px) ========== */
        @media (max-width: 370px) {
            html {
                font-size: 14px;
            }

            .container {
                padding: 0.5rem 0.45rem 1.25rem;
                gap: 0.9rem;
            }

            .header {
                padding: 1.2rem 0.7rem 1.1rem;
            }

            .header__name {
                font-size: 1.5rem;
            }

            .header__tagline {
                font-size: 0.78rem;
            }

            .section {
                padding: 0.9rem 0.7rem;
            }

            .section__title {
                font-size: 0.98rem;
            }

            .timeline__item {
                padding-left: 0.9rem;
            }

            .skills__tag {
                font-size: 0.7rem;
                padding: 0.18rem 0.4rem;
            }

            .edu__course-tag {
                font-size: 0.68rem;
                padding: 0.18rem 0.4rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">

        <!-- ==================== 头部 ==================== -->
        <header class="header">
            <div class="header__decoration" aria-hidden="true"></div>
            <div class="header__decoration header__decoration--right" aria-hidden="true"></div>
            <h1 class="header__name">黄乐天</h1>
            <p class="header__tagline">技术赋能财会学习</p>
            <div class="header__divider" aria-hidden="true"></div>
            <p class="header__school">
                <strong>北方民族大学</strong>&ensp;会计学专业&ensp;大二在读（2025级）&ensp;|&ensp;辅修法学
            </p>
        </header>

        <!-- ==================== 教育背景 ==================== -->
        <section class="section" aria-labelledby="sec-edu">
            <h2 class="section__title" id="sec-edu">教育背景</h2>
            <div class="edu__block">
                <div class="edu__school">北方民族大学</div>
                <div class="edu__detail">会计学（主修）&ensp;·&ensp;法学（辅修）&ensp;·&ensp;2025 年至今</div>
                <div class="edu__courses">
                    <span class="edu__course-tag">管理会计</span>
                    <span class="edu__course-tag">财务管理</span>
                    <span class="edu__course-tag">审计</span>
                    <span class="edu__course-tag">税法</span>
                    <span class="edu__course-tag">经济法</span>
                </div>
            </div>
        </section>

        <!-- ==================== 专业技能 ==================== -->
        <section class="section" aria-labelledby="sec-skills">
            <h2 class="section__title" id="sec-skills">专业技能</h2>
            <div class="skills__group">
                <div class="skills__group-title">财务与审计</div>
                <div class="skills__tags">
                    <span class="skills__tag">管理会计</span>
                    <span class="skills__tag">财务管理</span>
                    <span class="skills__tag">审计实务</span>
                    <span class="skills__tag">税法应用</span>
                </div>
            </div>
            <div class="skills__group">
                <div class="skills__group-title">法律基础</div>
                <div class="skills__tags">
                    <span class="skills__tag">经济法</span>
                    <span class="skills__tag">民商法</span>
                </div>
            </div>
            <div class="skills__group">
                <div class="skills__group-title">交叉能力</div>
                <div class="skills__tags">
                    <span class="skills__tag skills__tag--highlight">金融分析</span>
                    <span class="skills__tag skills__tag--highlight">法律合规评估</span>
                </div>
            </div>
            <div class="skills__group">
                <div class="skills__group-title">证书与备考</div>
                <div class="skills__tags">
                    <span class="skills__tag skills__tag--highlight">助理会计师（初级会计资格）</span>
                    <span class="skills__tag">大学英语四级（已通过）</span>
                    <span class="skills__tag">备考六级</span>
                    <span class="skills__tag">备考资产评估师</span>
                </div>
            </div>
            <div class="skills__group">
                <div class="skills__group-title">工具</div>
                <div class="skills__tags">
                    <span class="skills__tag">Excel</span>
                    <span class="skills__tag">用友</span>
                    <span class="skills__tag">金蝶</span>
                    <span class="skills__tag">办公软件</span>
                </div>
            </div>
        </section>

        <!-- ==================== 项目经历 ==================== -->
        <section class="section" aria-labelledby="sec-projects">
            <h2 class="section__title" id="sec-projects">项目经历</h2>
            <ul class="timeline">
                <li class="timeline__item">
                    <div class="timeline__header">
                        <span class="timeline__title">市场调研大赛</span>
                        <span class="timeline__role">数据负责人</span>
                        <span class="timeline__date">2025.11 — 2026.3</span>
                    </div>
                    <p class="timeline__desc">
                        担任数据负责人，负责问卷收集、数据清洗与分析，撰写数据部分报告；团队获校级奖项。
                    </p>
                </li>
                <li class="timeline__item">
                    <div class="timeline__header">
                        <span class="timeline__title">大学生创新创业项目（国家级）</span>
                        <span class="timeline__role">预算控制人</span>
                        <span class="timeline__date">2025.10 — 2026.5</span>
                    </div>
                    <p class="timeline__desc">
                        负责项目预算编制与成本控制，跟进资金使用效率，协助团队完成中期及结项报告。
                    </p>
                </li>
            </ul>
        </section>

        <!-- ==================== 实习经历 ==================== -->
        <section class="section" aria-labelledby="sec-internship">
            <h2 class="section__title" id="sec-internship">实习经历</h2>
            <div class="internship__card">
                <div class="internship__header">
                    <span class="internship__title">江陵县文旅局&ensp;财务助理</span>
                    <span class="internship__date">2026.1 — 2026.2</span>
                </div>
                <p class="internship__desc">
                    参与资产盘点、财务数据录入，协助文旅活动举办，熟悉政府财务工作环境与流程。
                </p>
            </div>
        </section>

        <!-- ==================== 实习与就业意向 ==================== -->
        <section class="section" aria-labelledby="sec-intent">
            <h2 class="section__title" id="sec-intent">实习与就业意向</h2>
            <div class="intent__grid">
                <div class="intent__item">
                    <div class="intent__label">意向领域</div>
                    <div class="intent__value">会计师事务所（审计 / 税务方向）</div>
                </div>
                <div class="intent__item">
                    <div class="intent__label">海外方向</div>
                    <div class="intent__value">海外财务岗位</div>
                </div>
            </div>
            <div class="intent__note">
                愿意接受高强度工作，能够快速适应多元文化环境，主动学习国际会计准则。
            </div>
        </section>

        <!-- ==================== 个人特质与兴趣 ==================== -->
        <section class="section" aria-labelledby="sec-traits">
            <h2 class="section__title" id="sec-traits">个人特质与兴趣</h2>
            <div class="traits__subtitle">特长</div>
            <ul class="traits__list">
                <li class="traits__list-item">细致可靠，能沉下心处理数据与分析</li>
                <li class="traits__list-item">团队组织协调能力强，多次担任项目负责人</li>
                <li class="traits__list-item">能快速适应政府、企业等不同工作环境</li>
            </ul>
            <div class="traits__subtitle">兴趣</div>
            <div class="interests__tags">
                <span class="interests__tag">经济</span>
                <span class="interests__tag">阅读（财务金融 · 历史人文 · 科学思辨）</span>
                <span class="interests__tag">旅游</span>
                <span class="interests__tag">健身</span>
            </div>
        </section>

        <!-- ==================== 页脚 ==================== -->
        <footer class="footer">
            <span>黄乐天</span>
            <span class="footer__dot" aria-hidden="true"></span>
            <span>北方民族大学</span>
            <span class="footer__dot" aria-hidden="true"></span>
            <span>会计学 · 法学</span>
        </footer>

    </div>
</body>
</html>
